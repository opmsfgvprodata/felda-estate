@model IEnumerable<MVC_SYSTEM.ViewingModels.vw_CutiUmumLdgDetails>
@using MVC_SYSTEM.Class;
@{
    ViewBag.Title = "_AllocatedPublicHolidayTable";
    //Layout = "~/Views/Shared/_LayoutMain.cshtml";
    GetConfig GetConfig = new GetConfig();
    //string statusPkj = GetConfig.GetWebConfigDesc(Model.fld_IsSelected, "statusaktif", Model.fld_NegaraID, Model.fld_SyarikatID);
    int bill = 0;
    //string YesNo = GetConfig.GetWebConfigDesc(, "pilihanYaTidak", fld_NegaraID, fld_SyarikatID);;
}

@if (Model.Count() > 0)
{
    <div id="scrollArea2">
        <logo />
        <table class="table table-hover table-bordered" style="font-size: 11px;" border="0">
            <thead>
                <tr>
                    <th colspan="10" style="text-align:center;vertical-align:middle;">
                        @ViewBag.NamaSyarikat
                        <br />
                        (@GlobalResEstate.lblCompanyNo : @ViewBag.NoSyarikat)
                        <br />
                        @GlobalResEstate.lblPublicHolidayInfo
                    </th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <th bgcolor="#ffcc80" style="color: black; text-align: center; vertical-align: middle !important; border: 1px solid black;" border="1">@GlobalResEstate.hdrNo</th>
                    <th bgcolor="#ffcc80" style="color: black; text-align: center; vertical-align: middle !important; border: 1px solid black;" border="1">@GlobalResEstate.hdrDesc</th>
                    <th bgcolor="#ffcc80" style="color: black; text-align: center; vertical-align: middle !important; border: 1px solid black;" border="1">@GlobalResEstate.hdrLeaveDate</th>
                    <th bgcolor="#ffcc80" style="color: black; text-align: center; vertical-align: middle !important; border: 1px solid black;" border="1">@GlobalResEstate.hdrLeaveTake</th>

                </tr>
                @foreach (var item in Model)
                {
                    bill = bill + 1;
                    <tr>
                        <td align="center" style="vertical-align: middle !important; border: 1px solid black;" border="1">
                            @bill
                        </td>
                        <td align="center" style="vertical-align: middle !important; border: 1px solid black;" border="1">
                            @item.fld_KeteranganCuti
                        </td>
                        <td align="center" style="vertical-align: middle !important; border: 1px solid black;" border="1">
                            @item.fld_TarikhCuti
                        </td>
                    </tr>
                }
            </tbody>

        </table>
        <newpage />
    </div>

    <div style="text-align:right; margin-top:10px">
        <a class="btn btn-default btn-sm" href="#" id="printpage">
            <span class="glyphicon glyphicon-print"> </span> @GlobalResEstate.btnPrint
        </a>
        <a class="btn btn-default btn-sm" href="#" id="downloadpdf">
            <span><img src="~/Asset/Images/pdf-icon.png" width="20px" height="20px" /></span> @GlobalResEstate.btnDownloadPdf
        </a>
    </div>

}
else
{
    <p style="color:red">@GlobalResEstate.msgErrorSearch</p>
}

